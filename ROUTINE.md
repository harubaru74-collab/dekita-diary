# 「できたこと日記」成長可視化の運用方法(2026-08-25〜：対話方式に変更)

## 経緯

最初はGoogle Driveへの手動保存＋毎時自動チェック（Routine）で作っていたが、
「待機中もトークンを消費するのが心配」「日記を書く工程自体もサイトで完結させたい」というフィードバックを受けて方式を変更した。

はるかちゃんは元々、その日の出来事をClaudeとの対話でまとめてもらう習慣があった（「チャッピー」との会話で「今日のできごと／できたこと／要約／チャッピーからのコメント」の形に整形してもらうスタイル）。この習慣はそのまま活かし、**そのまとめが完成した流れの中で、このリポジトリへの保存とサイトの再公開まで同じ会話内で完結させる**方式にした。

これにより：
- Google Driveへの保存が不要になった（対話の中で直接 `diary/YYYY-MM-DD.md` に保存する）。
- 常時ポーリングするRoutineが不要になった（会話が起きた時にしか動かないので、待機中のトークン消費はゼロ）。

## 日々の使い方

**主な窓口はClaude Cowork。**「Cowork」も中身はこのセッションと同じClaude Code基盤なので、Coworkだけで完結できる。次にCoworkを開いたら、最初に一度「GitHubの `harubaru74-collab/dekita-diary` を見て。`ROUTINE.md` の通りに、今日の日記をまとめたらそのままサイトも更新して」のように伝える（リポジトリ・Artifact・Google Driveアクセスは既にセットアップ済みなので、Cowork側での追加作業はほぼ不要なはず）。

1. はるかちゃんが、この `harubaru74-collab/dekita-diary` リポジトリにアクセスできるClaudeとの会話（Cowork、このセッション、またはこのリポジトリを開いた新しいセッションのいずれでもよい）で、いつも通り今日の出来事を話す。
2. Claudeが「原文ママ／今日のできごと／できたこと／要約／チャッピーからのコメント」の形にまとめる（これまでの `diary/*.md` の文体・トーンを参考にする）。
3. はるかちゃんが内容にOKを出したら、Claudeはその場で：
   a. `diary/YYYY-MM-DD.md` として保存する。
   b. 7日前・30日前・365日前の `diary/*.md` が存在するか確認する（無ければ「記録なし」として扱う）。
   c. `archive/shell.html` を土台に成長可視化ページを再構築する（詳細は [`archive/BUILD.md`](./archive/BUILD.md)）。チャッピーからのコメントは省略せず全文を`<details>`に入れる。
   d. Artifactツールで `url: "https://claude.ai/code/artifact/1a759afc-2415-432b-9d53-161c7c476f08"` を指定して再公開する（**非公開設定のまま**）。
   e. `diary/` の変更を `git add` → `git commit` → `git push`（ブランチ: main）。
4. 同じ会話の中で「ページを更新したよ」と伝えて終わり。自動通知やバックグラウンド処理は使わない。

## もう使わなくなったもの

- Google Drive フォルダ（`1dD_DwKfXys_sycnoS9H9FbOWu8yhqr4P`）からの自動取得：過去5日分（2026-08-16〜08-24）の初期データ移行だけに使った。今後の新規日記はここを経由しない。
- 毎時自動チェックのRoutine：2026-08-25に削除済み。トークンを待機中に消費する方式をやめた。

## 公開・非公開について（重要）

このプロジェクトは、日記の内容（恋愛・家族・退職相談など）が非常にプライベートであるため、**GitHub Pagesではなく非公開のClaude Artifactとして運用する**（2026-08-25、はるかちゃん本人の希望）。

- Artifactはデフォルトで非公開（自分しか見られない）。
- 誰かと共有したくなった場合は、はるかちゃん自身がArtifactの共有設定を変更する。会話の中で勝手に公開範囲を変えない。
- GitHubリポジトリ（このリポジトリ）自体も **Private** のまま維持すること。

## チャッピーコメントの扱いについて（重要）

成長可視化ページの「これまでの記録」アーカイブで、チャッピーからのコメントを「（以下略）」のように省略してしまったことがあった（2026-08-25、フィードバックで発覚・修正済み）。**メモを別途見に行かなくても、サイト上でクリック（`<details>`の展開）すれば全文が読めることが必須要件**。今後の追加でもこの点は絶対に省略しないこと。

## 運用上の注意

- このリポジトリは `/home/user/dekita-diary` にクローン済み（このセッション: `session_013awBmT1Z32nBZebtT21Bi3`）。別のセッションで作業する場合は、まず `add_repo` でこのリポジトリを追加してからクローンする。
- 参考実装：`roumu-news` リポジトリの `archive/BUILD.md`（Markdown→HTML変換・Artifact再公開の考え方は同じ）。ただし労務ニュースは公開ポートフォリオ用、こちらは非公開の個人用なので、公開範囲の扱いだけは明確に区別すること。
