# 「できたこと日記」成長可視化ルーティン(日記を書いたら自動更新)

## 目的
はるかちゃんが Google Drive に書いた「できたこと日記」を、今日・1週間前・1ヶ月前・1年前の同じ日付で並べて表示し、日々の成長を実感できるようにする。

## スケジュール(2026-08-25更新：固定時刻→差分チェック方式に変更)

- **毎時0分にチェックを実行**(このAI基盤のRoutineが安定してサポートする最短間隔が1時間のため、それより短い間隔では実行できない)。
- ただし実際に処理が走るのは**1日1回、その日初めて日記が見つかった時だけ**。`diary/YYYY-MM-DD.md`(今日の日付)が既に存在すれば即座に「処理済み」として何もせず終了するので、体感としては「日記を書いてから最大1時間弱以内にページが更新され、通知が届く」という動きになる。
- 今日の日記がまだ見つからない時間帯は、完全に無音(通知もpushもしない)で終了する。

## 実行内容（プロンプト仕様）

1. `diary/YYYY-MM-DD.md`（今日の日付）が既に存在するか確認する。
   - **存在する** → 今日の分は処理済み。何もせず終了（通知・pushしない）。
   - **存在しない** → 2へ進む。
2. Google Drive フォルダ（フォルダID: `1dD_DwKfXys_sycnoS9H9FbOWu8yhqr4P`）を検索し、今日の日付の日記ファイル（タイトル `YYYY-MM-DD のできごと` または `YYYY-MM-DD できたこと日記`）を取得する。
   - **見つからない**（まだ書いていない） → 何もせず終了（通知・pushしない）。次の毎時チェックで再確認する。
   - **見つかった** → 3へ進む（＝これが「日記を書いた直後の初回チェック」）。
3. 今日を基準に、7日前・30日前・365日前の日付を計算する。
4. それぞれの日付についてGoogle Driveを検索し、該当する日記があれば内容を取得する（無ければ「記録なし」として扱う）。
5. 今日の日記（と、まだバックアップしていない過去日の日記があれば）を `diary/YYYY-MM-DD.md` として保存する。保存ルールの詳細・HTML変換ルールは [`archive/BUILD.md`](./archive/BUILD.md) を参照。
6. `archive/shell.html` を元に成長可視化ページのHTMLを再構築し、Artifactツールで再公開する（**非公開設定のまま**。公開範囲は変更しない）。**チャッピーからのコメントは省略せず、必ず全文を`<details>`（クリックで展開）に入れる**。
7. `diary/` の変更を `git add` → `git commit` → `git push` する。
8. `PushNotification` ツールで完了を通知する（例：「🌱 成長日記が更新されたよ！」）。この通知は「今日初めて処理した時」だけ送る。

## データソースについて

- 保存場所：Google Drive「できたこと日記」フォルダ
- フォルダID：`1dD_DwKfXys_sycnoS9H9FbOWu8yhqr4P`
- ファイル形式：`YYYY-MM-DD のできごと`（text/plain）。ただし初期の数件は Google ドキュメント形式や `YYYY-MM-DD できたこと日記` という表記ゆれもあるため、日付一致を優先してタイトルを柔軟に判定すること。
- 同フォルダには日記以外のファイル（労務ニュースの下書きなど）が混入することがあるため、タイトルに日付＋「のできごと」または「できたこと日記」を含むものだけを対象にする。

## 公開・非公開について（重要）

このプロジェクトは、日記の内容（恋愛・家族・退職相談など）が非常にプライベートであるため、**GitHub Pagesではなく非公開のClaude Artifactとして運用する**と決めた（2026-08-25、はるかちゃん本人の希望）。

- Artifactはデフォルトで非公開（自分しか見られない）。
- 誰かと共有したくなった場合は、はるかちゃん自身がArtifactの共有設定を変更する。ルーティン側から勝手に公開範囲を変えない。
- GitHubリポジトリ（このリポジトリ）自体も **Private** のまま維持すること。

## チャッピーコメントの扱いについて（重要）

初回実装時、成長可視化ページの「これまでの記録」アーカイブでチャッピーからのコメントを「（以下略）」のように省略してしまう問題があった（2026-08-25、はるかちゃんからのフィードバックで発覚・修正済み）。**メモを別途見に行かなくても、サイト上でクリック（`<details>`の展開）すれば全文が読めることが必須要件**。今後の再構築でも、この点は絶対に省略しないこと。

## 運用上の注意

- このRoutineは、初期セットアップを行ったセッション（`session_013awBmT1Z32nBZebtT21Bi3`）に紐づけて作成した。新規セッションを毎回作る方式だと、このAI基盤の設定上コネクタ（Google Driveアクセス）を引き継げないため、あえて既存セッション宛てのRoutineにしている。
- そのため、このRoutineは元セッションが使える間は動き続けるはずだが、万一「Google Driveにアクセスできない」「セッションが見つからない」等のエラーが続く場合は、claude.ai の Routines管理画面から該当Routineを確認し、はるかちゃん自身のアカウントで作り直す（その場合はGoogle Driveコネクタを有効にすること）。
- Google Drive 連携が切れている・認証エラーが出た場合は、はるかちゃんに一度確認を取ってから再設定する。
- 参考実装：`roumu-news` リポジトリの `ROUTINE.md` / `archive/BUILD.md`（毎日のGitHub自動保存・Artifact再公開の仕組みは同じ考え方）。ただし労務ニュースは公開ポートフォリオ用、こちらは非公開の個人用なので、公開範囲の扱いだけは明確に区別すること。
