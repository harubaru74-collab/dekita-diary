# セットアップ手順（はるかちゃん用・あとで自分でやる分）

このリポジトリのコード・ワークフローはすべて用意済みです。残っているのは、
**Googleアカウント・GitHubアカウントでの操作**（Claudeが代わりにできない部分）だけです。
上から順番にやれば大丈夫です。

---

## ステップ1：Google Cloud でサービスアカウントを作る

「サービスアカウント」＝人間じゃなくてプログラムのための、専用のGoogleアカウントのようなもの。これを使ってGitHub Actionsが自分のGoogle Driveを読みに行きます。

1. https://console.cloud.google.com/ を開く（普段使ってるGoogleアカウントでログイン）。
2. 画面上部の「プロジェクトを選択」→「新しいプロジェクト」。
   - プロジェクト名：例）`dekita-diary`（何でもOK）
   - 「作成」をクリック。作成できたら、そのプロジェクトが選択された状態にする。
3. 左上のメニュー（≡）→「APIとサービス」→「有効なAPIとサービス」。
4. 上部の「+ APIとサービスを有効化」をクリック。
5. 検索窓に `Google Drive API` と入力→出てきた「Google Drive API」をクリック→「有効にする」。
6. 左メニュー→「APIとサービス」→「認証情報」。
7. 上部の「+ 認証情報を作成」→「サービスアカウント」を選択。
8. サービスアカウント名：例）`dekita-diary-reader` → 「作成して続行」→ 役割は特に設定せず「続行」→「完了」。
9. 作成されたサービスアカウントの一覧から、今作ったものをクリック。
10. 上部タブ「キー」→「鍵を追加」→「新しい鍵を作成」→ 形式は **JSON** を選択 →「作成」。
    - JSONファイルが自動的にダウンロードされる（例：`dekita-diary-xxxxx.json`）。
    - **このファイルは他人に絶対見せないこと**（GitHubにそのままコミットしないこと。ステップ3でGitHubの「シークレット」にだけ登録する）。
11. サービスアカウントの詳細画面の上の方に、`xxxxx@xxxxx.iam.gserviceaccount.com` のようなメールアドレスが書いてある。これを**コピーしてメモしておく**（ステップ2で使う）。

---

## ステップ2：日記フォルダをサービスアカウントと共有する

1. Google Drive で「できたこと日記」フォルダを開く。
2. フォルダを右クリック（またはメニュー）→「共有」。
3. ステップ1でメモした `xxxxx@xxxxx.iam.gserviceaccount.com` を追加。
4. 権限は「閲覧者」でOK（プログラムは読むだけなので）。
5. 「送信」または「共有」をクリック。

これで、このサービスアカウントがそのフォルダの中身を読めるようになりました。

---

## ステップ3：GitHubにサービスアカウントの鍵を登録する

1. https://github.com/harubaru74-collab/dekita-diary を開く。
2. 「Settings」タブ →左メニュー「Secrets and variables」→「Actions」。
3. 「New repository secret」をクリック。
4. **Name**: `GDRIVE_SA_KEY_JSON`
5. **Secret**: ステップ1でダウンロードしたJSONファイルの中身を、テキストエディタ等で開いて、**全部**コピーしてそのまま貼り付ける（`{` から `}` まで全部）。
6. 「Add secret」で保存。

---

## ステップ4：GitHub Pagesを有効にする

⚠️ **ここで公開範囲が決まります**。日記の内容はプライベートですが、GitHub PagesのURLは
「知っていれば誰でも見られる」形になります（検索エンジンには載らないよう `robots.txt` は設定済みですが、
リンクを知っている人には見えてしまいます）。URLを人に教えない・SNSに貼らない、で運用してください。

1. リポジトリの「Settings」タブ→左メニュー「Pages」。
2. 「Build and deployment」の「Source」を **「Deploy from a branch」** にする。
3. 「Branch」を **`main`**、フォルダを **`/docs`** にして「Save」。
4. 数分待つと、ページ上部に公開URLが表示される（例：`https://harubaru74-collab.github.io/dekita-diary/`）。これが成長可視化ページのURL。

---

## ステップ5：手動テスト実行

1. リポジトリの「Actions」タブを開く。
2. 左側の「できたこと日記 自動更新」ワークフローを選択。
3. 右側の「Run workflow」ボタン→ブランチ`main`のまま「Run workflow」。
4. 数十秒〜1分ほどで実行が終わるはず。✅ が付けば成功、❌ ならログを開いてエラー内容を確認する
   （よくある原因：サービスアカウントの共有忘れ、シークレット名のタイプミス、JSONの貼り付けミス）。
5. 成功したら、ステップ4のURLを開いて表示を確認する。

---

## ステップ6：日記を書いた瞬間に近い更新にする（任意、2026-08-25追加）

ここまでで「毎時0分・最大1時間弱のタイムラグ」の自動更新は完成している。
これは任意のステップで、**書いてから最短1分以内**に更新されるようにするための追加設定。

### 6-1. GitHubトークンを作る（すでに作成済みならスキップ）

1. https://github.com/settings/personal-access-tokens/new を開く
2. Token name：`dekita-diary-trigger` など
3. Repository access → Only select repositories → `dekita-diary`
4. Permissions → Repository permissions → **Actions** を **Read and write** に変更
5. 「Generate token」→ 表示された `github_pat_...` をコピーしてメモしておく（この画面を閉じると二度と見られない）

### 6-2. Google Apps Scriptを作る

1. https://script.google.com/ を開く（普段のGoogleアカウントでログイン）
2. 「新しいプロジェクト」
3. プロジェクト名を分かりやすく変更（例：`できたこと日記-即時トリガー`）
4. エディタに表示されているサンプルコードを全部消し、このリポジトリの
   [`scripts/apps_script/check_and_trigger.gs`](./scripts/apps_script/check_and_trigger.gs)
   の中身を全部コピーして貼り付ける
5. 左メニューの歯車アイコン「プロジェクトの設定」→ 一番下の
   **「スクリプト プロパティ」** →「スクリプト プロパティを追加」
   - プロパティ1：名前 `GITHUB_TOKEN` / 値 → 6-1で作ったトークンを貼り付け
6. エディタに戻り、上部の関数選択で `checkAndTrigger` を選び、実行ボタン（▶）を押す
   - 初回はGoogleの権限確認画面が出る（「このアプリは確認されていません」等）。
     「詳細」→「（プロジェクト名）に移動」→ Google Driveへのアクセスを許可する
   - エラーが出なければOK（初回実行時はほぼ確実にGitHub Actionsが1回起動する。これは正常な動作）

**既にステップ6を設定済みで、コードだけ更新したい場合**：6-2の4（コードの貼り付け）だけやり直せばよい。トリガーやScript Propertiesはそのまま使い回せる。

### 6-3. 1分おきに実行するトリガーを設定する

1. 左メニューの時計アイコン「トリガー」
2. 右下「+ トリガーを追加」
3. 実行する関数：`checkAndTrigger`
4. イベントのソース：**時間主導型**
5. 時間ベースのトリガーのタイプ：**分ベースのタイマー**
6. 時間の間隔：**1分おき**
7. 保存

これで、Google Drive側の「できたこと日記」フォルダの変化を1分おきにApps Scriptがチェックし、何か変化（新規保存・内容修正・日付訂正のリネーム・削除など）があれば即座にGitHub Actionsを起動してくれる。今日の日記に限らず、さかのぼって書いた過去日の日記（後追い投稿）や日付の訂正も対象になる（2026-09-05更新）。GitHub Actions側の毎時0分の定期チェックはそのまま残しておいてよい（保険として二重に動く形になるが、実害はない）。

---

## 日々の運用

- ステップ6まで設定した場合：Google Driveの日記フォルダに何か変化があると、**最短1分以内**にサイトが更新される（今日の分・さかのぼって書いた過去分・後からの内容修正や日付訂正、いずれも対象）。
- ステップ6を設定していない場合：**次の毎時0分のチェックのタイミング**で更新される（最大1時間弱のタイムラグ）。
- すぐに反映させたい場合は、ステップ5と同じ「Run workflow」で手動実行できる。
- この自動更新は **Claudeのトークンを一切消費しない**（GitHub Actions・Google Apps Scriptとも無料枠内で完結。
  リポジトリがPublicなのでGitHub Actionsの実行時間は無制限に無料）。
